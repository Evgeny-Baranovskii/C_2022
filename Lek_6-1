
#include <iostream>
#include <cstring>
using namespace std;

string reverse(string a)
{
   string b;
   b=a;
   int len=a.length();
   for(int i=0;i<len;i++)
   a[i]=b[len-1-i];
   return a;
}

int main()
{
   string a;
   cin»a;
   a=reverse(a);
   cout«a«endl;
}
