#include <iostream>
using namespace std;

int s
void reverse (char *a);

int main()
{
int s =strlen(a);
 char a[] = "ABCDE" ;
 reverse (a);
}
void reverse (char *a)
{
 if(*a)
 reverse (a+1);
 cout << *a;
}
